services:
  llservicios:
    image: shinsenter/laravel:latest

    container_name: llservicios
    volumes:
      - .:/var/www/html
    ports:
      - "82:80" # PHP-FPM escuchando en este puerto para que Nginx lo use
    networks:
      - redPrincipal
     
networks:
  redPrincipal:
    external: true

